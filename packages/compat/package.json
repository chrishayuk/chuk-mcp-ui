{
  "name": "@chuk/compat-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5199",
    "build": "echo 'compat-harness: no build needed (test-only package)'",
    "test": "playwright test --config tests/playwright.config.ts",
    "test:ui": "playwright test --config tests/playwright.config.ts --ui",
    "generate:fixtures": "tsx scripts/generate-fixtures.ts",
    "generate:manifest": "tsx scripts/generate-manifest.ts",
    "generate:matrix": "tsx scripts/generate-matrix.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.0",
    "@modelcontextprotocol/sdk": "^1.24.0",
    "@playwright/test": "^1.50.0",
    "ajv": "^8.17.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
